<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.eriko.updateditmmockup">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Activities.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Activities.EventInfobs" />
        <activity android:name=".Activities.ExhibitorListsbs" />
        <activity android:name=".Activities.SplashScreen" />
        <activity android:name=".Activities.MainMenu" />
        <activity android:name=".Fragments.ImageDetailsActivity" />
        <activity android:name=".Activities.Ticket" />
        <activity android:name=".Activities.RegisterAndLogin" />
        <activity android:name=".Activities.Register" />
        <activity android:name=".Activities.Login"></activity>
    </application>

</manifest>